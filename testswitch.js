// between each line we need to get this so can resume it later LOL

function exe() {
	let x = 10;
	let resume = x

	switch(resume) {
		case x:
				const k = 1;
				console.log('1')
		case x+1:
				console.log('2', k)

	}
}

exe()